export const goToSobreMim = (history) => {
    history.push("/sobremim")
}

export const goToProjetos = (history) => {
    history.push("/projetos")
}

export const goToHomePage = (history) => {
    history.push("/")
}